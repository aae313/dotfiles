#!/usr/bin/env bash
set -euo pipefail

BOOK_DIR="$HOME/misc/books"
cd "$BOOK_DIR" || exit 1

selection=$(fd -t f -e pdf -e epub | vicinae dmenu --placeholder 'Open Book: ')

if [ -n "$selection" ]; then
  zathura "$selection" &
fi
